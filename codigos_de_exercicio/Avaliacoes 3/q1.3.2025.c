#include <stdio.h>
#include <stdbool.h>
#define TAM 50
int main(){
    char text[TAM];
    int i = 0;
    printf("Digite as 5 palavras separadas por hifen:\n");
    fgets(text,TAM,stdin);
    while (text[i] != '\0'){
       
    }
    
}